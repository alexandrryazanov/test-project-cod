import styled from "@emotion/styled";

export const ContentWrapperWithMenu = styled.div`
    width: 100%;
    height: calc(100vh - 70px);
    display: flex;
    overflow-y: hidden;
`;

export const ContentWrapper = styled.div`
    width: 100%;
    padding: 25px;
    overflow-y: auto;
`;
